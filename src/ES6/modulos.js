// para importar debemos decir que es de donde es importante poner la extencion del archivo

//NOTA: si estamos usando Js sin frameworks o librerias que tengan modulos incorporados debemos agregar en el package.json debajo de la licencia "type": "module" para decir que acepte modulos

import hello from './module.js';

hello()